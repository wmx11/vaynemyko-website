<style is:global lang="scss">
  :root {
    --border-radius: 4px;

    --primary: 244 54% 63%;
    --primary-focus: 244 44% 33%;
    --secondary: 317 80% 54%;
    --secondary-focus: 317 80% 54%;
    --accent: 177 66% 69%;
    --accent-focus: 177 66% 69%;
    --black: 240 35% 7%;
  }

  html {
  }

  body {
    position: relative;
    background: hsl(var(--black));
    // color: #fff;
  }

  a:hover {
    text-decoration: underline;
  }

  .animate {
    opacity: 0;
  }

  nav {
    transition: 0.2s ease;

    &.open {
      height: 60%;
    }
  }

  @keyframes float {
    0% {
      transform: translateY(0px) rotate(0deg);
    }

    50% {
      transform: translateY(20px) rotate(10deg);
    }

    100% {
      transform: translateY(0px) rotate(0deg);
    }
  }

  .float {
    animation: float 8s ease infinite;

    @for $i from 1 through 10 {
      &:nth-child(#{$i}) {
        animation-delay: 800ms * $i;
      }
    }
  }
</style>
